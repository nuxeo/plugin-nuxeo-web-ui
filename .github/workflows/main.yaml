name: Main

on:
  pull_request:
    branches:
      - 10.10

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '8'

      # todo - we need to update the settings.xml file to use maven-internal

      # what should we do here, just call the cross-repo or directly run the functional tests here?